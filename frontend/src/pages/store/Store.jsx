import React from "react";
import { Link } from "react-router-dom";

const Store = () => {
    return (
        <div>
            <Link to="/Home">Home</Link>
        </div>
    );
};

export default Store;
